import 'package:flutter/material.dart';

// This file defines the custom colors used throughout the application.
// Defining colors in one place makes it easy to change the app's look later.

// Maroon color, used as the primary theme color (e.g., AppBars, Buttons).
const Color maroon = Color(0xFF800000);

// Silver color, often used for borders, secondary backgrounds, or accents.
const Color silver = Color(0xFFC0C0C0);
